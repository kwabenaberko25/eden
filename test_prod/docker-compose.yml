services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      DEBUG: "false"
      SECRET_KEY: "change-me-in-production"
